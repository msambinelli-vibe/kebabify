[package]
name = "kebabify"
version = "0.1.0"
edition = "2021"
description = "CLI to rename files to kebab-case or snake_case"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
walkdir = "2.5"
unicode-normalization = "0.1"

[dev-dependencies]
tempfile = "3.12"
